export type MessageType = {
  type?: string;
  text: string;
  timestamp?: string;
  sender?: string;
};

export type ContactType = {
  Id: string;
  Job: string;
  AvatarUrl: string;
  Name: string;
};
